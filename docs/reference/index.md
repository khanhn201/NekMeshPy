# API reference

The public API is re-exported from the top-level `nekmeshpy` package, so `from
nekmeshpy import ...` is stable regardless of file layout. The pages below are
generated from source docstrings.

```{toctree}
:maxdepth: 2

linemesh
trimesh
quadmesh
hexmesh
model
io
```
