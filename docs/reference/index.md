# API reference

The whole public API is re-exported from the top-level `nekmeshpy` package, so
`from nekmeshpy import ...` is stable regardless of the internal file layout. The
pages below are generated from the source docstrings — each leaf module is
documented once, at its canonical location.

```{toctree}
:maxdepth: 2

linemesh
trimesh
quadmesh
hexmesh
model
io
```
