# `linemesh`

The 1-D mesh sibling of `QuadMesh` / `HexMesh`: a shared `(N,3)` point array plus
branching `(L,2)` line connectivity, open or closed.

```{eval-rst}
.. automodule:: nekmeshpy.linemesh
   :members:
```
